{
  "name": "bluelive-maps",
  "version": "1.0.0",
  "description": "BlueLive Maps - AI-powered maps app on Cloudflare Workers with OpenStreetMap and 3D visualization",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.mjs",
    "dev": "npx wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241004.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.64.0"
  },
  "dependencies": {
    "itty-router": "^5.0.22"
  }
}
